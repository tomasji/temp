machine:
  environment:
    PATH: ${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin
  node:
    version: 0.10.18

dependencies:
  override:
      - bash -c 'echo "dependencies: OK"'


test:
  override:
          - ./run-test

